# dotfiles

## Set up repo on $HOME

```shell
sh -c "$(curl -fsSL --no-cache https://raw.githubusercontent.com/jblsp/dotfiles/main/install.sh)" && alias config='/usr/bin/git --git-dir=$HOME/.cfggit/ --work-tree=$HOME'
```
